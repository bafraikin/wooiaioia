/**
* all and (min-width: 400px)
*
* Anything more than 400px.
*/

html,
body {
    position: absolute;
    top: 0;
    left: 0;
    padding: 0;
    width: 100%;
    height: 100%;
}

html {
    overflow: hidden;
    background: #333 url( <%= image_path 'bg' %>) no-repeat right bottom;
    background-attachment: fixed;
    background-size: 100% auto;
}

body {
    background: url(<%= image_path 'bg1' %>) no-repeat left top, url(<%= image_path 'bg2' %>) no-repeat right top;
    background-color: transparent;
    background-attachment: fixed, fixed;
    background-size: auto 100%, auto 100%;
}

@media all and (min-height: 800px) {
    body {
        background: transparent url(<%= image_path 'bg2' %>) no-repeat right top;
        background-attachment: fixed;
        background-size: auto 75%;
    }
}

@media all and (min-height: 1366px) {
    body {
        background-size: auto 50%;
    }
}

@media all and (max-device-width: 600px) {
    body {
        background: transparent url(<%= image_path 'bg2' %>) no-repeat right -1px;
        background-attachment: fixed;
        background-size: auto 75%;
    }
}

.page-wrapper {
    position: relative;
    margin: 120px auto 200px auto;
    padding: 0 50px 20px 50px;
    width: 260px;
    background: url(<%= image_path 'layer-middle' %>) repeat-y center top;
    background-size: 360px auto;
    transition: margin 1s ease-out;
}

    .page-wrapper::before,
    .page-wrapper::after {
        position: absolute;
        left: 0;
        width: 360px;
        content: '';
    }

    .page-wrapper::before {
        top: -600px;
        height: 600px;
        background: url(<%= image_path 'hook3' %>) no-repeat center bottom, url(<%= image_path 'layer-frame' %>) no-repeat center 567px;
        background-size: 300px 20px, 360px auto;
    }

    .page-wrapper::after {
        bottom: -33px;
        height: 33px;
        background: transparent url(<%= image_path 'layer-frame' %>) no-repeat center bottom;
        background-size: 360px auto;
        -o-background-size: 360px auto;
        -webkit-background-size: 360px auto;
        background-size: 360px auto;
    }

